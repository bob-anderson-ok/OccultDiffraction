{
  // Set show_input to true if you want this file printed to
  // the console. Useful for debugging. No much good for anything else,
  // will probably be removed in future versions.

  show_input_bool : false,

  // Set this flag to false if you want the ground shadow PA to be
  // the same as the fundamental plane PA For Occult usage, this should always be false
  // for planned usage by Occult.
  // (It's not yet implemented anyway, so currently this setting doesn't matter.)

  rotate_ground_shadow_to_90_degree_pa_bool : false,

  // Set window_size to 0 to suppress display of 'user friendly' ground shadow

  window_size_pixels : 800,

  // Set path_to_external_image. This field should be omitted if no external image is supplied.
  // External images must be square and the width is used as fundamental_plane_width_num_points
  // and overrides any value supplied later for that parameter. Any ellipses defined will be
  // added on top of the external image. The expectation is that the image being supplied
  // is a polygon shape. The file format is 8 bit grayscale png with object pixels set to 0
  // and all background pixels set to 255 (actually, any non-zero value will work).

  // path_to_external_image : "input.png",

  // Set path_for_ground_shadow_output_folder to "" to suppress output files.
  // If a folder path is given, title will be used as the output file name
  // with spaces replaced by _ (underscore).
  // We have changed our minds about this issue and are now always outputting the 8 bit and 16 bit
  // .png files to the directory where the app is run from using fixed file names.
  // They are: occultImage16bit.png and displayImage8bit.png
  // NOTE: these are overwritten each time the app is run.

  // path_for_ground_shadow_output_folder : "C:/Users/boban/tool_test_folder",

  title : "(9203) Myrtus 2025 Feb 22",

  fundamental_plane_width_km : 40,
  fundamental_plane_width_num_points : 2000,  // Overridden if external image is supplied

  camera_exposure_secs: 0.040,  // Not yet implemented and will be ignored for Occult usage.

  // If you want an integrated (white light) ground shadow image, supply a path to a QE table file.
  // Use the example QHY174 sensor response curve file as a template. When present,
  // a ground shadow image will be generated for each entry in the table (use the stride
  // parameter to control how many entries are processed). Each image will be weighted by the QE
  // value at that wavelength for summing with the other images.

  path_to_qe_table_file : "C:/Users/boban/Dropbox/GolandProjects/IOTAtools/imx174QEapproxEvery5nm",

  // If a QE table is provided, the qe_table_stride parameter determines how many entries are processed
  // from the table. stride=1 will process all entries. stride=5 will process every 5th entry.

  qe_table_stride: 5,

  // If no QE table is provided, the ground image will be generated for the wavelength
  // specified below. This parameter is ignored if a QE table is provided.

  observation_wavelength_nm : 500,

  // The next 2 values determine shadow speed and fundamental plane PA (currently unused and not relevant
  // for Occult usage.

  dX_km_per_sec : 5.074,
  dY_km_per_sec : -0.904,

  star_name: "UCAC4 392-058875",
  //star_diam_on_plane_mas: 0.2,

  // These parameters control limb-darkening for the star.
  // Either the star class or a limb-darkening coefficient can be specified.
  // If both supplied, limb_darkening_coeff is used.
  // If omitted completely, no limb-darkening is applied.

  limb_darkening_coeff: 0.7,
  star_class : "B",

  // Distance to asteroid can be specified two ways. Use either form.
  // If both present, parallax_arcsec is used.

  parallax_arcsec : 3.7711,
  distance_au : 2.33,

  // This section can be omitted if an external image is supplied.
  // If included when an external image is supplied, the ellipse shape
  // will be added (overlaid) on top of the external image.

  main_body : {
       x_center_km : 5.8,
       y_center_km : 0.6,
       major_axis_km : 17.6,
       minor_axis_km : 8.0,
       major_axis_pa_degrees : 98.3,
  },

  // This section can be omitted if no satellite is present.
  satellite : {
       x_center_km : -5.8,
       y_center_km : -0.6,
       major_axis_km : 7.5,
       minor_axis_km : 2.8,
       major_axis_pa_degrees : 94.0,
  }
}